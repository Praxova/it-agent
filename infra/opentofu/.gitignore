# OpenTofu state (contains secrets and resource details)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Variable files with real credentials
terraform.tfvars
*.auto.tfvars

# Crash logs
crash.log
crash.*.log

# Override files (local dev overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.tfvars
.terraform/
*.tfstate*
