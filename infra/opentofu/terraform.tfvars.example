# ============================================================================
# terraform.tfvars.example
# ============================================================================
# Copy to terraform.tfvars and fill in your values.
# terraform.tfvars should be in .gitignore (contains secrets).
# ============================================================================

proxmox_api_token      = "packer@pam!packer-token=your-token-here"
windows_admin_password = "P@ssw0rd123!"
dsrm_password          = "P@ssw0rd123!"
